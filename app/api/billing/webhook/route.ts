import { NextResponse } from "next/server";

export async function POST() {
  // Paddle webhook scaffolding placeholder.
  // Add signature verification and event mapping in next phase.
  return NextResponse.json({ received: true });
}
